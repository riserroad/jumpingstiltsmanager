<form method="post" action="{{ path('jumping_tilt_delete', {'id': jumping_tilt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jumping_tilt.id) }}">
    <button class="btn">Delete</button>
</form>
