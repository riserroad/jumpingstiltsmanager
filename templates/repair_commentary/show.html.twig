{% extends 'base.html.twig' %}

{% block title %}RepairCommentary{% endblock %}

{% block body %}
    <h1>RepairCommentary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repair_commentary.id }}</td>
            </tr>
            <tr>
                <th>Repair_date</th>
                <td>{{ repair_commentary.repairDate ? repair_commentary.repairDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ repair_commentary.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('repair_commentary_index') }}">back to list</a>

    <a href="{{ path('repair_commentary_edit', {'id': repair_commentary.id}) }}">edit</a>

    {{ include('repair_commentary/_delete_form.html.twig') }}
{% endblock %}
