<form method="post" action="{{ path('repair_commentary_delete', {'id': repair_commentary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repair_commentary.id) }}">
    <button class="btn">Delete</button>
</form>
